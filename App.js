// Standalone hospital site lives in public/index.html.
// Keep React mount as a no-op so the React bundle does not override the static content.
function App() {
  return null;
}

export default App;
